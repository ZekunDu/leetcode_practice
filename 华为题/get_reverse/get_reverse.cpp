#include<iostream>
#include<string>

using namespace std;

int main(){
    string s = "10230";
    int n = s.size();
    int l = 0, r = n - 1;
    for (int x = 0; x < (n - 1) / 2; x++){
        for (int y = x + 1; x < (n + 1) / 2; y++){
            bool revered = false;
            while (l < r){

            }
            if (reversed){

            }
        }
    }

    return 0;
}
